---
label: Tags
hide_body: true
fields:
- name: select_tags
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: documents
      section: data
      file: data/tags.yaml
      path: tags_test
    options:
    - squarespace
    - squarespace 7.1
  label: select tags
- name: tags_test
  type: tag_list
  default: []
  label: tags test
pages:
- data/tags.yaml
