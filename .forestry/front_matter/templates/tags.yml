---
label: Tags
hide_body: true
fields:
- name: select_tags
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: documents
      section: data
      file: data/tags.yaml
      path: tags-test
    options:
    - squarespace
    - squarespace 7.1
  label: select tags
pages:
- data/tags.yaml
