---
label: Authors
hide_body: true
fields:
- name: map
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: data
  label: map
- name: authors
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: false
    label: Name
  - name: bio
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: Bio
  - name: author_image
    type: file
    config:
      maxSize: 64
    label: Author Image
  config:
    min: 
    max: 
    labelField: name
  label: Authors
  hidden: false
pages:
- data/authors.yaml
