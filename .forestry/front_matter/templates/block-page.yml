---
label: Block Page
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: blocks
  type: blocks
  label: Blocks
  template_types:
  - hero-block
  config:
    min: 
    max: 
